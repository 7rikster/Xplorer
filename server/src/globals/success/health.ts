import * as Utils from "../../utils";

const api = Utils.Response.success("XPLORER API");

// const pong = Utils.Response.success("Pong", 200);
const pong = Utils.Response.success("Pong");

export { api, pong };
